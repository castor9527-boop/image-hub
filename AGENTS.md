# 项目协作规则

## 修改前确认

当用户提出页面、功能、接口或结构修改时，必须先确认以下内容，再开始编辑：

1. 修改范围：明确涉及的页面、组件、接口、数据或配置文件。
2. 修改目标：明确期望的行为、视觉结果和验收标准。
3. 保留内容：明确哪些现有功能、结构、文案、数据和样式必须保持不变。
4. 暂不修改内容：明确哪些相关但未被要求的部分不得顺手调整。

如果需求可能影响多个模块，先向用户列出影响范围和实施方向，等待确认后再修改。只有紧急故障修复可以直接处理；修复后必须说明实际改动的文件、原因和影响。

## 变更纪律

- 修改必须保持在已确认的范围内。
- 不得因为修复一个局部问题而重构、回滚或替换整个页面、工作台或启动架构。
- 启动配置、构建配置、依赖版本、路由和数据结构属于高影响变更，必须单独说明并获得确认。
- 修改前先检查工作区状态，不能覆盖用户已有的未提交改动。
- 完成后说明修改内容、未修改内容和验证结果。
